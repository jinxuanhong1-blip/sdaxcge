# w200 / B4 — GSE126044 CLDN4 and TJ score recompute

Run from the repo root:

```bash
python3 scripts/w200/B4_GSE126044_recompute.py
```

Writes tables, figures, and `summary.json` under `results/w200/B4_GSE126044/`.
See that folder’s `WRITEUP.md` for the honest R vs NR result
(claimed p = 0.019 is **not** recovered).
