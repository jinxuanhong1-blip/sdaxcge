# STORY GAP FILL — Tacstd2 → barrier/junction (honest #1–3)

Public-only reanalysis. Smooths the Tacstd2 → junction sentence **without**
claiming false “KEGG TJ #1”, and **without** claiming PR #744 is wrong.

```bash
pip install -r requirements.txt
python3 analyze.py
```

See `FINDING.md` for the verdict.
