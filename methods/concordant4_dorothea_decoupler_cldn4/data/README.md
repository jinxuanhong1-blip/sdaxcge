Malignant pseudobulk UMI sums and the PR #503 quartile table (`sample_inventory.tsv`).

These are the same processed public matrices used for the concordant-4 IFN/MHC gene scores. They are not single-cell counts. P4001 is absent (`in_count_matrix=false` in the inventory; that row is not copied into the TF n).

`a8_sets.json` supplies the Hallmark IFN, MHC-I/APM, tight-junction, and OXPHOS gene lists for the non-TF comparison.
