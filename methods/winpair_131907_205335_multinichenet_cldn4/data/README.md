Public GEO processed files and NicheNet-v2 priors are downloaded to `/tmp/winpair_multinichenet_cldn4` by `scripts/00_download.py`. They are not committed.

`lr_network.tsv` is the converted NicheNet-v2 human LR network (Zenodo 7074291).
