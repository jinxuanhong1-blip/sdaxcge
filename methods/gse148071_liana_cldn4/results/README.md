Results written by `scripts/01_run_liana.py`.

Human-readable LR table and honest n: `../FINDING.md` (copy also here).
Large per-patient pair tables are kept so the Wilcoxon ranks can be audited.
The 1.15 GB TISCH2 h5 is gitignored under `../data/`.
