# Example

The committed `results/` tables are the real public-data run, not a toy.

For a planted-effect check that does not use GEO:

```bash
python3 ../scripts/selftest_dm.py
```

That recovers a synthetic TNK-down slope (n=40 Dirichlet-multinomial draws).
It is a software test, not a biological result.
