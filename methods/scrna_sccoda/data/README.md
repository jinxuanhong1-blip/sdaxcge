# Local GEO downloads (gitignored)

Populated by `scripts/download.py` from NCBI GEO FTP. Public supplementary
files only.

- `GSE207422/` UMI matrix + metadata; `GSE207422_markers.tsv.gz` is the
  extracted lineage-gene slice.
- `GSE241934/` IIT + Real MTX, features, barcodes, author metadata.
- `GSE291670/` `GSE291670_RAW.tar` and extracted 10x MTX.
