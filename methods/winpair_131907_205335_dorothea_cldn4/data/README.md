Public GEO files are downloaded to `/tmp/geo_winpair/` (not committed).

- GSE131907 UMI + author annotation + series matrix
- GSE205335 UMI RDS + author identity + family SOFT

See `scripts/download.py`. The 2.86 GB GSE131907 log2TPM text and all
FASTQ/EGA files are skipped.
