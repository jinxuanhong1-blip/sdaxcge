`download.py` fetches the three public GEO processed matrices.
`analyze.py` builds one kNN graph per dataset and writes the stacked
nhood table plus `FINDING.md`. `knn_nhood.py` is the documented
Spearman + SpatialFDR fallback (not miloR).
