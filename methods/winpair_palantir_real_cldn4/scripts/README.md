`download.py` fetches public processed UMIs.
`extract_epithelium.py` builds the joint epithelial AnnData.
`analyze.py` installs-requires Palantir and writes destiny/pseudotime tables.
`gene_sets.py` locks CLDN4 / barrier (no CLDN4) / IFN (no CLDN4).
