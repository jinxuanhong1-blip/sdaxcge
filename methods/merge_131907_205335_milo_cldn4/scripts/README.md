# Scripts

- `download.py` — public GSE131907 + GSE205335 processed UMIs (<2 GB each).
- `knn_nhood.py` — documented kNN neighbourhoods + k-distance SpatialFDR
  (miloR fallback; not edgeR).
- `run_merge.py` — per-dataset graphs, optional Harmony extra, writes
  `FINDING.md` and `tables/nhoods.tsv`.
