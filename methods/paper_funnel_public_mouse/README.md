# Paper funnel — public mouse (advisor / PPT)

Public matrices only. **No private 8KL.** Series are never merged with private data.

Reproduce:

```bash
bash methods/paper_funnel_public_mouse/scripts/download.sh
python3 methods/paper_funnel_public_mouse/scripts/analyze.py
```

See `FINDING.md` for the PPT-aligned answers.
