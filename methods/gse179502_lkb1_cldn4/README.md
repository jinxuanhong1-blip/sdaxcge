# GSE179502 — Lkb1 restore, Cldn4-only (mouse)

Public sorted neoplastic scRNA (mtx + features + barcodes on GEO).
See `FINDING.md`.

```bash
python3 methods/gse179502_lkb1_cldn4/analyze.py
```

GEO files cache under `data/` and are gitignored (mtx ~183 MB).
