# Python environment for the Visium track (templates 01-05) and demo helpers.
# Visium 流程（模板 01-05）与演示脚本的 Python 环境。
# Pin ranges reflect the 2024-2026 tool generation named in the playbook.
# 版本范围对应手册所述 2024-2026 工具代际。
#
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements-visium.txt
#
# GPU note: cell2location/scvi-tools train much faster on a CUDA GPU. Install the
# matching torch build for your CUDA version first if using GPU.
# GPU 提示：cell2location/scvi-tools 在 CUDA GPU 上快得多；如用 GPU，请先按 CUDA
# 版本安装匹配的 torch。

scanpy>=1.10,<1.12
anndata>=0.10
squidpy>=1.7            # neighbourhood enrichment, co-occurrence, spatial graphs (v1.8.2 = Jun 2026)
cell2location>=0.1.4    # 0.1.4 (2024), 0.1.5 (2025)
scvi-tools>=1.1
SpatialDE>=1.1.3        # SpatialDE2 API: `import SpatialDE; SpatialDE.test(...)`
scikit-learn>=1.3
scikit-misc>=0.3        # required by scanpy highly_variable_genes (seurat_v3/pearson)
leidenalg>=0.10
igraph>=0.11
pandas>=2.0
numpy>=1.24
scipy>=1.11
matplotlib>=3.8
pyyaml>=6.0
