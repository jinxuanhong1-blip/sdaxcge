# scripts

| File | Role |
|---|---|
| `knn_nhood.py` | Graph, refined indices, DA, SpatialFDR (same fallback as the single-cohort CLDN4 Milo folders) |
| `download.py` | Public GEO files for **GSE131907 + GSE148071 only** |
| `run_pair.py` | Per-dataset graphs, sample-level pair tests, extra figures, `FINDING.md`, nhood table |

GSE205335 is out of scope. Dual-high TACSTD2+CLDN4 is not a gate.
