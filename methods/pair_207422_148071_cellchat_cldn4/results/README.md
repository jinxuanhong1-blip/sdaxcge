# results

Required deliverables:

- `combo_rho.tsv` — patient-level Spearman ρ per cohort and DerSimonian–Laird combo (primary CellChat definition + sensitivities).
- `lr_table.tsv` — consensus ligand–receptor pairs (significant in both kept splits, same ΔP sign).

Also: `lr_table_outgoing.tsv`, `honest_n.tsv`, `patient_scores.tsv`, `summary.json`, extra `fig_*.png`.
