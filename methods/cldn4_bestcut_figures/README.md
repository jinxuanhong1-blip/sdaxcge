# CLDN4 best-cut extra figure pack

Additive redraw of already-significant public CLDN4 extras. No re-audit.

```bash
python3 methods/cldn4_bestcut_figures/make_figures.py
```

Outputs land in `figures/`. Numbers are locked in `locked_numbers.tsv`. See `FINDING.md`.
