# Python side of the GRN/CCC playbook (LIANA, pySCENIC downstream, pseudobulk).
# Versions below are the ones the demo was validated against (Python 3.12).
# Pin to reproduce; relax cautiously.

scanpy==1.12.3
anndata==0.13.2
liana==1.8.1
leidenalg==0.12.0
scipy==1.18.0
statsmodels==0.14.6
pandas==2.3.3
numpy==2.4.4
plotnine==0.15.8
pyyaml>=6.0
openpyxl>=3.1          # GSE207422 sample metadata xlsx

# --- optional but recommended (install as needed) ---------------------------
# scikit-image        # Otsu thresholding in 00_preprocess.py
# scrublet            # doublet detection (or use scDblFinder in R)
# harmonypy           # batch integration for the embedding only
# decoupler           # pseudobulk + differential CCC helpers
# pyscenic            # SCENIC regulon inference (heavy; needs cisTarget DBs)
# loompy              # SCENIC I/O
# celltypist          # reference-based annotation
