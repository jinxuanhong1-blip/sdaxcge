# results

Written by `../analyze.py`.

- `combo_rho.tsv` — pair Spearman ρ and Q4 vs Q1 r (primary + %pos sensitivity)
- `combo_lr_table.tsv` — outgoing Mal → T/NK join
- `ligand_table.tsv` — significant-in-either plus pre-specified key pairs
- `singles.tsv` — per-cohort atomic rows
- `pair_units.tsv` — the 12 + n_mal≥20 units behind the combo
- `summary.json` — machine-readable headline numbers
