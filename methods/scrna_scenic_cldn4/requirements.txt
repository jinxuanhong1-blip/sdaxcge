numpy>=1.26
pandas>=2.1
scipy>=1.11
matplotlib>=3.8
openpyxl>=3.1
statsmodels>=0.14
# Full pySCENIC cisTarget is not run (no motif DBs / no invented ChIP).
# pyscenic and arboreto are optional and unused by default.
