# Leftover OPEN lung ST — CLDN4 vs CD8A

Public spatial only. CLDN4-only. Skip controlled / skip if CLDN4 absent.

```
python3 methods/leftover_open_st/analyze.py --only stereo
python3 methods/leftover_open_st/analyze.py --only gse301973
python3 methods/leftover_open_st/analyze.py --only hd
```

Inputs live under `data/leftover_open_st/` (gitignored). Outputs: `results/leftover_open_st/`.
