# Kras-mutant mouse lung ICB: Cldn4 vs response and NHEJ/IFN

Public GEO matrices plus the TISMO lung inventory. LLC is not labeled KL.

```bash
python3 methods/kras_lung_icb_cldn4_sweep/analyze.py
```

Expression files are downloaded to `$KRAS_LUNG_ICB_DATA` (default `/tmp/kras_icb`). Tables and figures land in `results/kras_lung_icb_cldn4_sweep/`.
