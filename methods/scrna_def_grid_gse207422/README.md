# GSE207422 public-UMI definition grid

ADDITIVE extra on public GEO UMI only. Exhaustive malignant × immune × score grid for TACSTD2 vs T/NK-family fractions and NMPR vs MPR.

A3 slide numbers are taken as given (NMPR>MPR; ρ ≈ −0.40 to −0.50). This slice does not re-litigate the slide. Author CopyKAT IDs are not public.

```bash
python3 methods/scrna_def_grid_gse207422/scripts/download.py
python3 methods/scrna_def_grid_gse207422/scripts/analyze.py
```

See `playbook.md` for definitions, `REPORT.md` for every ρ / p / n, and `fig_def_grid.png` for the extra grid figure. Gold boxes mark ρ ≤ −0.35 or NMPR>MPR.
