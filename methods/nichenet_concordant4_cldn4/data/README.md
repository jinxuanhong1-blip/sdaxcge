Locked concordant-4 unit tables (patient or sample), copied from the CellChat concordant-4 run so the NicheNet split uses the same inventory.

`lr_network_human_21122021.tsv` is the NicheNet v2 human ligand–receptor table (Zenodo 7074291). The ligand–target matrix is downloaded by `scripts/download.sh` and is not stored here.
