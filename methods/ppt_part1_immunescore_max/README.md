# PPT Part 1: maximum |ρ| vs ImmuneScore

Locked 221-gene CLDN4-high signature (PR 590 order) and TACSTD2 versus the 8-gene ImmuneScore on OncoSG and the PR 590 GEO LUAD bulks. The reported maximum is the largest absolute cross-study Spearman inside that grid. ESTIMATE immune ssGSEA is a labeled sensitivity. GSE10072, GSE11969, and GSE248378 are not opened.

```bash
python3 methods/ppt_part1_immunescore_max/analyze.py
```

Matrices download to `/tmp/cldn4sig` and are not committed. The write-up is `FINDING.md`.
