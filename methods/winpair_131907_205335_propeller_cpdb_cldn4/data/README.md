# Locked extracts (winning pair only)

- `GSE131907_samples.tsv` — PR #320 / #279 T/NK extract (sample-level).
- `GSE205335_patients.tsv` — PR #320 / #279 T/NK extract (patient-level).
- `GSE131907_sample_metadata.tsv` — GEO sample → patient map (PR #347).
- `GSE205335_gsm_sample_metadata.csv` — GEO GSM → patient / `orig.ident` (PR #362).
- `GSE131907_tls_patients.tsv` — PR #274 TLS patient-level B companion (not the primary unit).

Matrices are downloaded to `/tmp` and are not stored here.
