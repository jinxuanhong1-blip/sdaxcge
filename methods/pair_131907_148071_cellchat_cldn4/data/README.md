Locked public extracts. GSE205335 is **not** in this pair.

- `GSE131907_samples.tsv` — PR #320 / #279 T/NK extract (sample-level counts + malignant CLDN4). After the tumor-origin + ≥20 malignant + ≥20 T/NK floor this is one sample per patient.
- `GSE131907_sample_metadata.tsv` — GEO series sample → `patient_id` (Kim et al.).
- `GSE148071_per_sample.tsv` — PR #348 CellChat per-biopsy epithelial / T/NK counts and CLDN4 (marker-argmax epithelium).
