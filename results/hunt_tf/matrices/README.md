Compact recount3 count extracts (hunt genes + library size) for the primary audit set: GTEx lung, TCGA LUAD/LUSC, the three raw SUPPORTED hits, EVLP lung, and the cell-line catalogues.

The other ~230 scored cohorts are not stored here. Re-fetch them with `python3 scripts/03_fetch_matrices.py` (writes the full set under `results/hunt_tf/matrices` and `$HOME/hunt_data/matrices`).
