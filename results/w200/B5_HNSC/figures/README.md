# No association figure generated

No eligible public cohort contains joinable CLDN4 expression and patient-level
RECIST ORR. Plotting NIVACTOR CLDN4 values against inferred response labels
would fabricate a primary result the public files do not support.

GSE179730 leftover values exist (`../leftover_per_sample.tsv`) but are not
plotted here: the locked outcome is pathologic/hybrid response, radiographic
RECIST PR count is 0, and n=3 leftover responders would overstate a non-RECIST
exploratory contrast. See `../candidate_audit.tsv` and `../leftover_statistics.tsv`.
